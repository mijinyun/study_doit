//Array.isArray()

let user = {
    name: 'Kim',
    age: 30,
};

let userList = ['Kim','Tom','Jane'];

console.log(typeof user);
console.log(typeof userList);

console.log(Array.isArray(user));
console.log(Array.isArray(userList));
